server {
    listen 0.0.0.0:8000;

    location / {
        proxy_set_header Host $host:$server_port;
        proxy_pass http://0.0.0.0:8001;
    }

    location /static/ {
        autoindex on;
        alias /vagrant/site/app/staticfiles/;
    }
}
